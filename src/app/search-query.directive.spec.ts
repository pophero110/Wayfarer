import { SearchQueryDirective } from './search-query.directive';

describe('SearchQueryDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchQueryDirective();
    expect(directive).toBeTruthy();
  });
});
